# Unilog Product Intelligence Engine — dependencies
# ==================================================
# Core engine runs on the Python 3 standard library only.
# Streamlit is required only for the cloud-hosted demo (share.streamlit.io).
streamlit>=1.30.0
